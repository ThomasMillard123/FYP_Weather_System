#include "AirMass.h"

FrontType AirMass::IsHit(AirMass* airMass)
{
    //Warm into cold= warm
    //cold into Hot= Cold
    // power = power = StationaryFront
    //Dir = Dir (OccludedFront) may not be done

    return FrontType();
}
